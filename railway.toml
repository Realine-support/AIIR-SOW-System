[build]
builder = "nixpacks"

[build.nixpacksPlan]
providers = ["python"]

[build.nixpacksConfig]
pythonVersion = "3.11"

[deploy]
startCommand = "cd aiir-sow-system && uvicorn api.index:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
